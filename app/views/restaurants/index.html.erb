<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to "#{restaurant.name}", restaurant_path(restaurant) %></li>
  <% end %>
</ul>

<%= link_to 'New Restaurant', new_restaurant_path(@restaurant), class: "btn btn-primary" %>
